<header id="main-header">
  <div id="logo"><a href="/">The Library</a></div>
  <nav>
    <ul class="nav-items">
      <% if (!locals.isAdmin) { %>
        <li><a href="/">Catalogue</a></li>
      <% } %>
    
      <% if (locals.isAuth && !locals.isAdmin) { %>
        <li><a href="/orders">Orders</a></li>
      <% } %>
    
      <% if (locals.isAdmin) { %>
        <li><a href="/admin/products">Manage Catalogue</a></li>
        <li><a href="/admin/orders">Manage Orders</a></li>
      <% } %>
    
      <% if (!locals.isAuth) { %>
        <li><a href="/signup">Signup</a></li>
        <li><a href="/login">Login</a></li>
      <% } %>
    
      <% if (locals.isAuth) { %>
        <li>
          <form action="/logout" method="POST">
            <button>Logout</button>
          </form>
        </li>
      <% } %>
    </ul>
  </nav>
    <!-- Hamburher menu for mobiles -->
    <button id="mobile-menu-btn">
      <span></span>
      <span></span>
      <span></span>
    </button>
</header>



<!-- The aside is for the mobile option. Its just the same nav items as before -->
<aside id="mobile-menu">
  <nav>
    <ul class="nav-items">
      <% if (!locals.isAdmin) { %>
        <li><a href="/">Catalogue</a></li>
      <% } %>
    
      <% if (locals.isAuth && !locals.isAdmin) { %>
        <li><a href="/orders">Orders</a></li>
      <% } %>
    
      <% if (locals.isAdmin) { %>
        <li><a href="/admin/products">Manage Catalogue</a></li>
        <li><a href="/admin/orders">Manage Orders</a></li>
      <% } %>
    
      <% if (!locals.isAuth) { %>
        <li><a href="/signup">Signup</a></li>
        <li><a href="/login">Login</a></li>
      <% } %>
    
      <% if (locals.isAuth) { %>
        <li>
          <form action="/logout" method="POST">
            <button>Logout</button>
          </form>
        </li>
      <% } %>
    </ul>
  </nav>
</aside>