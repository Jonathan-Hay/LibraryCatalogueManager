<%- include('../includes/head', { pageTitle: book.title }) %>
  <link rel="stylesheet" href="/styles/products.css">
  </head>

  <body>
    <%- include('../includes/header') %>
      <main id="product-details">
        <header>
          <img src="<%= book.imageUrl %>" alt="<%= book.title %>">
          <div id="product-info">
            <h1>
              <%= book.title %>
            </h1>
            <p><%= book.availability %>
            </p>

            <% if (book.availability === 'Unavailable - currently on Loan') { %>
              <h3>This book is currently on loan!</h3>
              <% } else { %>
                <form action="/orders" method="POST">
                  <button class="btn" name = "id" value = "<%= book.id %>">Loan Book</button>
                </form>

            <% } %>


          </div>
        </header>

        <p id="product-description">
          <%= book.description %>
        </p>
      </main>
  </body>

  </html>